## 1.1.0

* Branding API → `BotChatTheme` / Flutter `ThemeData`.
* Bot icon in header and bot bubbles (`showIcon`, `botIconUrl`).
* Attachments: camera, gallery, documents with Kore chunked upload + WS send.
* Speech-to-text (mic) and text-to-speech (speaker toggle).
* Footer visibility flags from config and branding v3.

## 1.0.0

* Initial pure Flutter Kore Bot SDK.
* STS → jwtgrant → RTM WebSocket networking (no native UI bridge).
* Chat UI with full template parity vs Flutter Public New Android holders.
* `KoreBotChat.open` API compatible with legacy MethodChannel config keys.
